import type { NextConfig } from "next";

const nextConfig = {
  images: {
    domains: [
      'viamarket.vtexassets.com',
      'http2.mlstatic.com',
      'images.unsplash.com',
      'images.fravega.com'
    ],
  },
};

export default nextConfig;
